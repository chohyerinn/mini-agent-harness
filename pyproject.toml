# harness 자체의 pytest 설정.
# testpaths를 tests/로 고정해, 루트에서 `pytest`를 돌려도 과제 워크스페이스의
# 테스트(tasks/*/tests/, PYTHONPATH 없이는 import 불가)를 수집하려다 깨지지 않게 한다.
# 과제 테스트는 harness가 격리된 서브프로세스에서 따로 실행한다(harness/scoring.py).
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
